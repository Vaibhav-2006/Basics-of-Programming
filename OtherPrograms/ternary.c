#include<stdio.h>
int main()
{
	int a,b,c;
	printf("Enter values of a and b:");// greater of two nos.
	scanf("%d %d",&a,&b);
		c=((a>b)?a:b);
	printf("The bigger no. is: %d",c);
		printf("The bigger no. is: %d",a>b);
	return 0;
}
